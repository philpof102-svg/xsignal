{
  "name": "xsignal",
  "version": "0.1.0",
  "private": true,
  "description": "x402-paid real-time X/social signal (scored + cited) for AI agents — an ingredient for the agentic economy on Base. Verify-only, never signs.",
  "scripts": {
    "start": "node app.js",
    "example": "node examples/watchlist-alerter.js",
    "test": "node signal.js && node tokenintel.js && node brief.js && node intent.js && node x402.js && node sources.js && node app.js --selftest"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@coinbase/x402": "^2.1.0"
  }
}
